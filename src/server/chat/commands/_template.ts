const config = {
    description: `An amazing command!`,
    aliases: [],
    usage: ``
};

const run = async (message: string, args: string[], chatter, chatUsers: any[]) => {

};

export {
    config,
    run
};
